.claude/
.copilot/
.codex/
.env/
.internal/
# medical volumes — hundreds of MB, never in git
*.nii
*.nii.gz
*.dcm
*.DCM
*.zip

# per-machine path roots — every machine has its own
paths.local.yaml

# local data + scratch outputs + venv
data/
outputs/
.venv/
